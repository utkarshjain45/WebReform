spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/webreform_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

optimizer:
  service:
    url: ${OPTIMIZER_SERVICE_URL:http://optimizer:8000}
